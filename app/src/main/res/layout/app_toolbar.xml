<?xml version="1.0" encoding="utf-8"?>

<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="?attr/colorSurface"
    android:elevation="4dp"
    android:foregroundTint="@color/button_list"
    android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_scrollFlags="scroll|enterAlways"
    app:title="@string/none"
    app:titleTextAppearance="@android:style/TextAppearance.Material.Large"
    app:titleTextColor="@color/appBarForeground"
    >
    <ImageButton
        android:id="@+id/mainSettingsButton"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:contentDescription="@string/none"
        android:background="?attr/actionBarItemBackground"
        android:layout_marginEnd="@dimen/margin_medium"
        android:src="@drawable/settings_24px"
        app:tint="@color/appBarForeground" />
</androidx.appcompat.widget.Toolbar>